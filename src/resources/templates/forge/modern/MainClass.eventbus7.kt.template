package {{packageName}}

import net.minecraftforge.common.MinecraftForge
import net.minecraftforge.fml.common.Mod
import net.minecraftforge.fml.event.lifecycle.FMLCommonSetupEvent
import net.minecraftforge.fml.javafmlmod.FMLJavaModLoadingContext
import org.apache.logging.log4j.LogManager
import org.apache.logging.log4j.Logger

@Mod("{{modId}}")
class {{projectName}}(context: FMLJavaModLoadingContext) {
    companion object {
        private val LOGGER: Logger = LogManager.getLogger()
    }

    init {
        val modBusGroup = context.modBusGroup
        FMLCommonSetupEvent.getBus(modBusGroup).addListener { event: FMLCommonSetupEvent -> setup(event) }
        MinecraftForge.EVENT_BUS.register(this)
    }

    private fun setup(event: FMLCommonSetupEvent) {
        LOGGER.info("Hello from Modern Forge (Kotlin)!")
    }
}